name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: dotnet restore SaaSProjectManager.sln
      - name: Build
        run: dotnet build SaaSProjectManager.sln --no-restore --configuration Release
      - name: Test
        run: dotnet test SaaSProjectManager.sln --no-build --configuration Release --verbosity normal

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend/saas-project-manager-web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/saas-project-manager-web/package-lock.json
      - name: Install
        run: npm ci
      - name: Build
        run: npm run build
